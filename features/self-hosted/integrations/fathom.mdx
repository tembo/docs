---
title: 'Fathom'
description: 'Connect Fathom MCP tools to your self-hosted Tembo instance.'
---

## Prerequisites

Complete the [shared prerequisites](/features/self-hosted/integrations/overview) and prepare a Fathom account with access to a test recording. Your API and sandboxes must be able to reach the provider's MCP service and its authentication endpoints.

## Connect Fathom

1. Open **Settings → Integrations** on your own Tembo instance.
2. Select **Fathom** and start the installation.
3. Complete the provider authorization if prompted. Review the requested access and select the intended account or workspace.
4. Confirm the connection is ready before starting a new session.

The catalog server endpoint is `https://api.fathom.ai/mcp`. The catalog type is `fathom-mcp` if your administrator restricts suggested servers.

For OAuth, your instance uses its own API callback, normally `https://tembo.example.com/api/oauth/mcp/callback`. If the provider requires an explicitly registered client, follow the [self-hosted MCP authentication instructions](/features/self-hosted/integrations/mcp#oauth-on-your-own-domain).

## Verify

Start a new session and ask Tembo to summarize a non-sensitive test meeting. Confirm the tools are available and that any returned account data belongs to the account you intended to connect.

## Access and troubleshooting

Authorize the intended user account. A recording that was not shared with that account will not become visible simply because Tembo is self-hosted.

If the card is missing, check your release and catalog settings. If the connection remains pending, finish authorization from your own instance. For connectivity failures, check DNS, outbound policy, and TLS trust from both the API and sandbox. See [MCP setup](/features/self-hosted/integrations/mcp) for custom servers and connection visibility.
