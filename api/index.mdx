---
title: 'Overview'
description: 'Create sessions, manage repositories, and automate workflows programmatically.'
---

<Card title="Using Tembo self-hosted?" icon="server" href="./self-hosted">
  Configure the API for your self-hosted Tembo deployment.
</Card>

## Authentication

Generate an API key from **Settings > API Keys** in the [dashboard](https://app.tembo.io).

```bash
curl -H "Authorization: Bearer YOUR_API_KEY" \
  https://api.tembo.io/v1/sessions
```

## Rate limits

100 requests per minute per API key. 1,000 requests per hour per organization.
