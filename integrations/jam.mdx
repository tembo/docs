---
title: 'Jam'
description: 'Read Jam bug reports, recordings, and reproduction steps through MCP.'
---

Read Jam bug reports, recordings, and reproduction steps through MCP.

## Prerequisites

You need access to your Tembo organization and access to a Jam workspace and a test recording. Review the provider permissions before granting access to your data.

## Installation

1. Open **Settings → Integrations** in Tembo.
2. Select **Jam** and start the installation.
3. Complete the provider authorization if prompted. Review the requested access and select the intended account or workspace.
4. Confirm the connection is ready before starting a new session.

The catalog server endpoint is `https://mcp.jam.dev/mcp`.

If authorization requires a pre-registered OAuth client, follow the [MCP authentication instructions](/integrations/mcp#advanced) and register the callback shown in the connection dialog.

## Usage and verification

Start a new session and ask Tembo to read a test bug report and summarize its reproduction steps. Confirm the tools are available and that any returned account data belongs to the account you intended to connect.

## Access and troubleshooting

Recordings can contain sensitive screen content. Connect only the workspace whose recordings agents should access.

If the connection remains pending, return to **Settings → Integrations** and finish authorization. If tools do not appear, confirm the connection was saved and start a new session. See [MCP troubleshooting](/integrations/mcp#advanced) for authentication and connectivity checks.
